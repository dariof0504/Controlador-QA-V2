const { createSlice } = require("@reduxjs/toolkit");

const estresRutinaSlice = createSlice({
    name:'estresSlice',
    initialState: {},
    reducers: {}
})

export default estresRutinaSlice.reducer